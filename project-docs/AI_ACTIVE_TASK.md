# AI ACTIVE TASK — D2-WP004-R1-R2-R2 FINAL XLSX TEMPLATE SEMANTIC EVIDENCE CORRECTIVE AUTHORIZED

Mode: **LOW-CREDIT / BOUNDED / ONE-SHOT / EVIDENCE-ONLY / EXACT ONE EXISTING MARKDOWN FILE / OWNER-TEMPLATE READ-ONLY / NO SOURCE / NO TEST / NO PROFILE / NO KINTONE / NO DEPLOY / D3 HOLD**  
Branch: `ai/antigravity-wp002c`  
Updated: 2026-09-02 ICT

Fresh-fetch current HEAD first. Fast path: `D2_REVIEW_FAST_START.md` -> this file -> directly relevant accepted evidence/authority -> exact corrective diff.

## 1. Current truth
```text
OWNER_OBJECTIVE = COMPLETE D2 TO PASS / CLOSED BEFORE D3
D1 = PASS / CLOSED
D2 = IN PROGRESS
D2_PRESERVATION = PASS / CLOSED
D2_REFERENCE_IMAGE = PASS / CLOSED
D2_PART_A_STRUCTURAL = PASS / CLOSED
D2_PART_B_STRUCTURAL = PASS / CLOSED
D2_FORMULA_AUTHORITY = PASS / CLOSED
D2_PART_B_EXPANDED_PRIVACY = PASS / CLOSED
CONTROL_PLANE_REVIEW_CORRECTIVE_STANDING_AUTH = EXHAUSTED / DO NOT REUSE
ANTIGRAVITY_AUTO_AUTH = NO
CLAUDE_AUTO_REVIEW = NO
ACTIVE_WORK_PACKAGE = D2-WP004-R1-R2-R2
ACTIVE_WORK_PACKAGE_NAME = FINAL XLSX TEMPLATE SEMANTIC EVIDENCE CORRECTIVE
TASK_STATE = AUTHORIZED / WAIT ANTIGRAVITY EVIDENCE CORRECTIVE
OWNER_APPROVAL_BASELINE_HEAD = 549553aaf69f737df4cdae7213f96e9dcad930aa
ACTIVE_D2_SOURCE_CHANGE_AUTH = NONE
ACTIVE_D2_TEST_CHANGE_AUTH = NONE
ACTIVE_D2_EVIDENCE_WRITE_AUTH = D2-WP004-R1-R2-R2-EVIDENCE-20260902-01
ACTIVE_KINTONE_WRITE_AUTH = NONE
ACTIVE_DEPLOY_AUTH = NONE
ANTIGRAVITY = AUTHORIZED ONLY FOR R1-R2-R2 EVIDENCE / ONE-SHOT / LOW-CREDIT
CLAUDE = STOP
PRODUCTION_RENDERER = NOT AUTHORIZED
D3 = HOLD
```

This Owner authorization is execution-only for exactly one evidence-corrective or blocker commit. Independent review begins only after Owner says `review`.

## 2. Authorization identity
```text
WORK_PACKAGE = D2-WP004-R1-R2-R2
AUTHORIZATION_TOKEN = D2-WP004-R1-R2-R2-EVIDENCE-20260902-01
MODE = EVIDENCE-ONLY / BOUNDED / ONE-SHOT / SAME ONE EXISTING MARKDOWN FILE
OWNER_APPROVAL_BASELINE_HEAD = 549553aaf69f737df4cdae7213f96e9dcad930aa
EXPECTED_COMMITS = EXACTLY ONE EVIDENCE-CORRECTIVE OR BLOCKER COMMIT AFTER THIS AUTHORIZATION COMMIT
```

Authorized writable repository file ONLY:
`project-docs/phase-3/evidence/XLSX_TEMPLATE_SEMANTIC_MAPPING_EVIDENCE.md`

No second file. No source/test/profile/template-binary modification.

## 3. Prior review truth / frozen accepted evidence
R1-R2-R1 authorization: `D2-WP004-R1-R2-R1-EVIDENCE-20260902-01`  
R1-R2-R1 evidence: `26fa18feead191c7587df82e393c73366969000d`

Freeze/retain unless direct contradictory accepted owner-template/repository evidence exists:

### Owner-template identity
```text
PART_A_SHA256 = 03d1e8c32bacea9277a8725010237eb46b46dd5f3b7799db7b8b89c3f6e28ef3
PART_B_SHA256 = c210c049ccc1daa83449f08c41276d4a668d1518864c7780a72e611ae15ed5b3
```

### Part A Hoshin
```text
G16:AF19 = partA.hoshin.departmentHoshinTitle
AM16:BI19 = partA.hoshin.sectionHoshinTitle
G8:S8 = appraisal-period/static content / NOT Hoshin
```

### Part A headers
```text
Department = Z7:AF7 -> partA.header.department
Section = AG7:AL7 -> partA.header.section
Position = BD7:BI7 -> partA.header.position
Employee Code = AQ7:AS7 -> partA.header.employeeCode
Primary Name-Surname = AT7:BC7 -> partA.header.employeeName
employeeNameTH alternate source selection = UNRESOLVED
```

Projection fields `profileCode`, `profileFamily`, `partAWeightPercent` exist but lack distinct proven header targets and remain `UNRESOLVED / NO_PROVEN_WORKBOOK_TARGET`.

### Part B headers
Accepted mappings remain:
- fiscal year `G2:H3` -> `partA.header.fiscalYear`;
- department `J3:L3` -> `partA.header.department`;
- section `M3:O3` -> `partA.header.section`;
- position `P3:Q3` -> `partA.header.position`;
- employee code `R3` -> `partA.header.employeeCode`;
- employee name `S3:W3` -> `partA.header.employeeName`.

### Structural/privacy authority
```text
SELF AREA = K:Q
CHIEF AREA = R:X
row30/34/38 = protected as previously frozen
summary relocation = N6 31:34 / N7 35:38 / N8 39:42
```

### Conservative taxonomy already accepted
- final score/grade without static owner-template semantic proof remain unresolved;
- Part B standalone overall/comment/feedback/signature regions without secured service source remain `NO_SECURED_PROJECTION_SOURCE`;
- no projection field is labeled `NO_SECURED_PROJECTION_SOURCE` merely because workbook target is absent.

Do not reopen these accepted items merely to spend credit.

## 4. Exact final corrective contract
Correct ONLY the remaining defects below.

### A. Restore complete Part B Chief authority
Current evidence headings say Chief `R:X` but competency rows still show narrower `R:W` targets.

Required truth:
```text
FROZEN_CHIEF_DYNAMIC_AUTHORITY = R:X
```

If exact visual merge/value cell is narrower than the dynamic authority, record them separately:
- `visual target/merge` = what owner template visually proves;
- `frozen chief dynamic authority` = exact `R:X`.

Do NOT present `R:W` as the complete chief authority.

Chief secured key/path remains `UNRESOLVED` unless a direct accepted stable `competencyItems` field key/path is proven. Do not reconstruct chief values for Employee-Self.

### B. Remove SAFE_TO_MAP without a secured projection path
Current evidence marks `SUMMARY_WEIGHT_SUM` with secured path `N/A` as `PROVEN / SAFE_TO_MAP`.

This is forbidden.

Mandatory invariant:
```text
SAFE_TO_MAP_WITH_NULL_OR_UNKNOWN_SECURED_PATH = 0
```

Formula Authority forbids recreating formula/scoring logic in Excel. Unless an exact existing secured projection path supplies a renderable value and workbook semantic ownership is proven, `SUMMARY_WEIGHT_SUM` is not a production write mapping and must be downgraded to `UNRESOLVED / KEEP_UNRESOLVED` or otherwise non-writable without inventing a source.

### C. Evaluator role identity: cite direct accepted authority or downgrade
Current evidence marks:
- template `1st Appraiser` -> `managerAchievement/managerScore`;
- template `2nd Appraiser` -> `gmAchievement/gmScore`;
as `PROVEN / SAFE_TO_MAP` without an accepted role-identity citation.

For each evaluator translation:
- if an exact directly relevant accepted repository/workflow authority explicitly proves the identity, cite the exact document/source in `Evidence Source` and rationale;
- otherwise downgrade to `UNRESOLVED / KEEP_UNRESOLVED`.

Do not infer by naming intuition. Do not conduct broad repository scans. If direct accepted authority is not readily available, mark unresolved immediately.

The same rule applies to Appraisee/Appraiser/Chief/Final aliases.

### D. Combined B:I objective/target region cannot prove title-only ownership by itself
Evidence states B:I is a combined objective/title/target visual region while standalone `OBJECTIVE_i_TITLE` remains `PROVEN / SAFE_TO_MAP`.

Unless an exact accepted composition/source-selection rule defines deterministic content for B:I, standalone `title` must be downgraded to `UNRESOLVED / KEEP_UNRESOLVED`.

Do not weaken the already conservative `description`/`target` unresolved decisions. Apply the same principle to any other combined region where one secured field is selected without explicit composition authority.

### E. Remove premature Baseline self-promotion
Executor evidence must not say `PROVEN EVIDENCE BASELINE`, `PASS`, `CLOSED`, or equivalent before independent review.

Use neutral status such as:
`EVIDENCE CANDIDATE / AWAITING INDEPENDENT REVIEW`.

### F. Recalculate final decision counts mechanically
After edits, recalculate:
- `PROVEN`;
- `UNRESOLVED`;
- `NO_SECURED_PROJECTION_SOURCE`;
- duplicate exclusive `SAFE_TO_MAP` target count.

Do not reuse old `24 / 16 / 5` counts unless they remain mechanically true.

Final invariants:
```text
DUPLICATE_EXCLUSIVE_SAFE_TO_MAP_TARGETS = 0
SAFE_TO_MAP_WITH_NULL_OR_UNKNOWN_SECURED_PATH = 0
PROVEN_FROM_PROXIMITY_OR_ALIAS_INTUITION = 0
CHIEF_FROZEN_AUTHORITY = R:X
SOURCE_TEST_PROFILE_CHANGE = 0
EVIDENCE_SELF_PROMOTION = 0
```

## 5. Read-only authorities
Use only directly relevant accepted authority as needed:
- exact SHA-approved Part A and Part B owner XLSX templates READ-ONLY;
- existing semantic evidence markdown to correct it;
- `src/services/mbo-export-service.js` READ-ONLY as secured projection authority;
- `src/core/kintone-normalizer.js` READ-ONLY only to confirm field existence, NOT evaluator role identity;
- frozen structural/privacy Baselines;
- directly relevant accepted workflow/role authority only if already available and needed to prove evaluator identity.

No broad repository search is required or desired. If role/composition authority is not directly available, use `UNRESOLVED`.

Owner XLSX may be loaded for inspection but must never be saved or mutated.

## 6. Explicit restrictions
Do NOT modify:
- `src/profiles/mbo-xlsx-template-profile.js`;
- `tests/mbo-xlsx-template-profile.test.js`;
- `src/services/mbo-export-service.js`;
- `src/core/kintone-normalizer.js`;
- feasibility source/tests;
- Baseline/control documents during executor work;
- package files/dependencies;
- `dist/`;
- owner-template XLSX binaries;
- any repository file other than the authorized evidence markdown.

Do NOT:
- create/edit Production XLSX Renderer;
- generate XLSX/PDF/image evidence artifacts;
- mutate workbook cells/styles/merges/defined names/formulas/media/package XML;
- recalculate scoring;
- change authorization logic;
- touch Kintone/ACL/process/customization;
- deploy;
- run Live UAT;
- invoke Claude;
- start Combined Excel parity, PDF parity, export security regression or D3.

No personal employee names/codes/comments/scores may be copied into evidence.

## 7. Verification and commit contract
Before commit run:
```bash
git diff --name-only
git status --porcelain
```

Exactly ONE repository path may be changed:
`project-docs/phase-3/evidence/XLSX_TEMPLATE_SEMANTIC_MAPPING_EVIDENCE.md`

Then:
- create exactly ONE evidence-corrective OR blocker commit;
- commit contains exactly the one authorized markdown file;
- push to `ai/antigravity-wp002c`;
- STOP immediately;
- do not self-declare PASS/CLOSED/Baseline;
- do not edit profile/source afterward;
- do not start next gate.

Report only:
- commit SHA;
- exact changed file;
- Part A SHA result if rechecked;
- Part B SHA result if rechecked;
- PROVEN count;
- UNRESOLVED count;
- NO_SECURED_PROJECTION_SOURCE count;
- duplicate exclusive SAFE_TO_MAP count;
- SAFE_TO_MAP with null/unknown path count;
- `git status --porcelain`;
- blocker if any.

## 8. Authorization ledger / exact next action
```text
D2-WP004-R1-SOURCE-TEST-20260902-01 = CONSUMED / CORRECTIVE / DO NOT REUSE
D2-WP004-R1-R1-SOURCE-TEST-20260902-01 = CONSUMED / CORRECTIVE / DO NOT REUSE
D2-WP004-R1-R2-EVIDENCE-20260902-01 = CONSUMED / CORRECTIVE / DO NOT REUSE
D2-WP004-R1-R2-R1-EVIDENCE-20260902-01 = CONSUMED / CORRECTIVE / DO NOT REUSE
D2-WP004-R1-R2-R2-EVIDENCE-20260902-01 = ACTIVE / ONE-SHOT / EVIDENCE-ONLY / EXACT ONE EXISTING MARKDOWN FILE
CONTROL-PLANE-D2-REVIEW-CORRECTIVE-20-ROUND-20260901 = EXHAUSTED / DO NOT REUSE
ACTIVE_D2_SOURCE_CHANGE_AUTH = NONE
ACTIVE_D2_TEST_CHANGE_AUTH = NONE
ACTIVE_D2_EVIDENCE_WRITE_AUTH = D2-WP004-R1-R2-R2-EVIDENCE-20260902-01
ACTIVE_KINTONE_WRITE_AUTH = NONE
ACTIVE_DEPLOY_AUTH = NONE
NEXT_EXECUTOR = ANTIGRAVITY
NEXT_ACTION = EXECUTE ONLY D2-WP004-R1-R2-R2-EVIDENCE-20260902-01; CORRECT EXACTLY ONE EVIDENCE MARKDOWN; ONE COMMIT; PUSH; REPORT; STOP
ANTIGRAVITY = AUTHORIZED ONLY FOR R1-R2-R2 EVIDENCE
CLAUDE = STOP
KINTONE = NONE
DEPLOY = NONE
PRODUCTION_RENDERER = NOT AUTHORIZED
D3 = HOLD
```
