# AI ACTIVE TASK — D2-WP004-R1-R2-R1 XLSX TEMPLATE SEMANTIC EVIDENCE CORRECTIVE AUTHORIZED

Mode: **LOW-CREDIT / BOUNDED / ONE-SHOT / EVIDENCE-ONLY / EXACT ONE EXISTING MARKDOWN FILE / OWNER-TEMPLATE READ-ONLY / NO SOURCE / NO TEST / NO PROFILE / NO KINTONE / NO DEPLOY / D3 HOLD**  
Branch: `ai/antigravity-wp002c`  
Updated: 2026-09-02 ICT

Fresh-fetch current HEAD first. Fast path: `D2_REVIEW_FAST_START.md` -> this file -> directly relevant Baseline/evidence -> exact owner-template re-inspection.

## 1. Current truth
```text
OWNER_OBJECTIVE = COMPLETE D2 TO PASS / CLOSED BEFORE D3
D1 = PASS / CLOSED
D2 = IN PROGRESS
D2_PRESERVATION = PASS / CLOSED
D2_REFERENCE_IMAGE = PASS / CLOSED
D2_PART_A_STRUCTURAL = PASS / CLOSED
D2_PART_B_STRUCTURAL = PASS / CLOSED
D2_FORMULA_AUTHORITY = PASS / CLOSED
D2_PART_B_EXPANDED_PRIVACY = PASS / CLOSED
CONTROL_PLANE_REVIEW_CORRECTIVE_STANDING_AUTH = EXHAUSTED / DO NOT REUSE
ANTIGRAVITY_AUTO_AUTH = NO
CLAUDE_AUTO_REVIEW = NO
ACTIVE_WORK_PACKAGE = D2-WP004-R1-R2-R1
ACTIVE_WORK_PACKAGE_NAME = XLSX TEMPLATE SEMANTIC EVIDENCE CORRECTIVE
TASK_STATE = AUTHORIZED / WAIT ANTIGRAVITY EVIDENCE CORRECTIVE
OWNER_APPROVAL_BASELINE_HEAD = 3c9f0dc3f0528ba1cddf2122090eaf0f094f7ada
ACTIVE_D2_SOURCE_CHANGE_AUTH = NONE
ACTIVE_D2_TEST_CHANGE_AUTH = NONE
ACTIVE_D2_EVIDENCE_WRITE_AUTH = D2-WP004-R1-R2-R1-EVIDENCE-20260902-01
ACTIVE_KINTONE_WRITE_AUTH = NONE
ACTIVE_DEPLOY_AUTH = NONE
ANTIGRAVITY = AUTHORIZED ONLY FOR R1-R2-R1 EVIDENCE / ONE-SHOT / LOW-CREDIT
CLAUDE = STOP
PRODUCTION_RENDERER = NOT AUTHORIZED
D3 = HOLD
```

This Owner authorization is execution-only for exactly one evidence-corrective or blocker commit. Independent review starts only when Owner says `review`.

## 2. Authorization identity
```text
WORK_PACKAGE = D2-WP004-R1-R2-R1
AUTHORIZATION_TOKEN = D2-WP004-R1-R2-R1-EVIDENCE-20260902-01
MODE = EVIDENCE-ONLY / BOUNDED / ONE-SHOT / SAME ONE EXISTING MARKDOWN FILE
OWNER_APPROVAL_BASELINE_HEAD = 3c9f0dc3f0528ba1cddf2122090eaf0f094f7ada
EXPECTED_COMMITS = EXACTLY ONE EVIDENCE-CORRECTIVE OR BLOCKER COMMIT AFTER THIS AUTHORIZATION COMMIT
```

Authorized writable repository file ONLY:
`project-docs/phase-3/evidence/XLSX_TEMPLATE_SEMANTIC_MAPPING_EVIDENCE.md`

No second file. No source/test/profile/template-binary modification.

## 3. R1-R2 review truth / freeze accepted evidence
R1-R2 authorization: `D2-WP004-R1-R2-EVIDENCE-20260902-01`  
R1-R2 evidence: `6e7cb1f5633dfc2a85dc181ae37f425dab3ea067`

Freeze/retain unless direct owner-template re-inspection proves contradiction:

### Owner-template identity
```text
PART_A_SHA256 = 03d1e8c32bacea9277a8725010237eb46b46dd5f3b7799db7b8b89c3f6e28ef3
PART_B_SHA256 = c210c049ccc1daa83449f08c41276d4a668d1518864c7780a72e611ae15ed5b3
```

### Part A Hoshin evidence
```text
G16:AF19 = partA.hoshin.departmentHoshinTitle = PROVEN
AM16:BI19 = partA.hoshin.sectionHoshinTitle = PROVEN
G8:S8 = appraisal-period/static content; NOT Hoshin
```

### Part B header evidence
- fiscal year `G2:H3` -> nested `partA.header.fiscalYear`;
- department `J3:L3` -> nested `partA.header.department`;
- section `M3:O3` -> nested `partA.header.section`;
- position `P3:Q3` -> nested `partA.header.position`;
- employee code `R3` -> nested `partA.header.employeeCode`;
- employee name `S3:W3` -> nested `partA.header.employeeName`.

### Existing frozen structural/privacy authority
- Part B K:Q = self-evaluation dynamic rating area;
- Part B R:X = chief-evaluation dynamic rating area;
- original rows7:29 dynamic;
- row30 protected;
- N7 inserted31:33 dynamic + row34 protected;
- N8 inserted31:33 and35:37 dynamic + rows34/38 protected;
- summary destinations N6=31:34, N7=35:38, N8=39:42.

R1-R2 token is consumed/corrective/do not reuse. Submitted R1-R2 evidence is NOT yet a durable proven semantic baseline.

## 4. Read-only authorities
Read only as needed:
- exact SHA-approved Part A and Part B owner XLSX templates;
- existing `project-docs/phase-3/evidence/XLSX_TEMPLATE_SEMANTIC_MAPPING_EVIDENCE.md` to correct it;
- `src/services/mbo-export-service.js` as secured projection authority;
- `src/profiles/mbo-xlsx-template-profile.js` only to identify disputed/current claims, never as semantic truth;
- `scripts/export/mbo-xlsx-ooxml-feasibility.js` only for already-proven structural/privacy regions;
- `CONFIRMED_BASELINE/D2_PART_A_STRUCTURAL_CLOSURE.md`;
- `CONFIRMED_BASELINE/D2_PART_B_STRUCTURAL_CLOSURE.md`;
- `CONFIRMED_BASELINE/D2_FORMULA_AUTHORITY_CLOSURE.md`;
- `CONFIRMED_BASELINE/D2_PART_B_EXPANDED_PRIVACY_CLOSURE.md`;
- `CONFIRMED_BASELINE/EXPORT_TEMPLATE_MAPPING_ARCHITECTURE.md`;
- `phase-3/evidence/COMPETENCY_SOURCE_EVIDENCE.md` only where relevant;
- directly relevant accepted workflow/role authority only when needed to prove evaluator identity.

Do not reinterpret or weaken frozen authorities.

## 5. Exact R1-R2-R1 evidence corrective contract

### A. Re-verify owner-template identity before semantic re-inspection
Independently calculate SHA-256 for the exact local owner templates before using them.

Required exact values:
```text
PART_A_SHA256 = 03d1e8c32bacea9277a8725010237eb46b46dd5f3b7799db7b8b89c3f6e28ef3
PART_B_SHA256 = c210c049ccc1daa83449f08c41276d4a668d1518864c7780a72e611ae15ed5b3
```

If either template is unavailable or SHA mismatches:
- do NOT substitute another workbook;
- do NOT infer semantics;
- correct the one authorized evidence file into a blocker record using `BLOCKER_TEMPLATE_SOURCE_NOT_AVAILABLE` or `BLOCKER_TEMPLATE_SHA_MISMATCH`;
- commit/push the one file;
- STOP.

### B. Preserve accepted Hoshin and Part B header evidence
Retain the accepted SHA/Hoshin/Part B-header evidence above unless direct re-inspection of the exact SHA owner template proves a concrete contradiction. Do not spend credit re-deriving already-frozen evidence unnecessarily.

### C. Rebuild Part A header matrix from exact label/merge ownership
Re-inspect every relevant Part A row-6 static label and value merge/range, including at minimum:
```text
N6:Q7
Z7:AF7
AG7:AL7
AM7:AP7
AQ7:AS7
AT7:BC7
BD7:BI7
```

For each candidate record:
- exact static label text;
- label cell/range;
- candidate writable cell/range;
- exact merge relationship;
- exact secured projection path if present;
- status and rationale.

No duplicate independent semantic may own the same exclusive `SAFE_TO_MAP` target unless an explicit accepted composition/source-selection contract proves shared ownership.

Specifically correct the prior invalid duplicate claim:
- `HEADER_DEPARTMENT -> AG7` and
- `HEADER_SECTION -> AG7`.

Do not infer ownership from horizontal proximity.

### D. Resolve employeeName vs employeeNameTH source-selection truth
The workbook visibly has one Name-Surname target. Separate:
1. workbook semantic ownership of that one target; and
2. which secured source field should populate it.

Do NOT mark both `partA.header.employeeName` and `partA.header.employeeNameTH` as independent writable owners of the same target.

If an existing accepted repository fallback/source-selection rule explicitly determines primary/fallback behavior, cite it and record one production write semantic with that source-selection rule. If no such authority exists, keep alternate source-selection `UNRESOLVED`.

### E. Correct status taxonomy
Use exactly:
- `PROVEN` = workbook semantic/address ownership AND compatible secured projection meaning are both proven;
- `UNRESOLVED` = workbook target/meaning, source selection, role translation, field composition, or other required semantic authority remains ambiguous/insufficient;
- `NO_SECURED_PROJECTION_SOURCE` = workbook semantic target is identifiable, but current secured `MboExportService` truly exposes no corresponding source field/path.

The following current secured paths exist and therefore must NOT be labeled `NO_SECURED_PROJECTION_SOURCE` merely because no workbook target is visible:
- `partA.header.profileCode`;
- `partA.header.profileFamily`;
- `partA.header.partAWeightPercent`.

If no distinct proven workbook target exists, use `UNRESOLVED` with rationale `NO_PROVEN_WORKBOOK_TARGET`.

### F. Complete Part A objective/evaluation inventory
The final evidence must contain an explicit row for EVERY current secured objective field below, even where `UNRESOLVED`:
- `title`;
- `description`;
- `kpi`;
- `target`;
- `measurement`;
- `weight`;
- `progressPercent`;
- `actualResult`;
- `selfAchievement`;
- `selfComment`;
- `managerAchievement`;
- `managerScore`;
- `managerComment`;
- `gmAchievement`;
- `gmScore`;
- `gmComment`;
- `averageScore`.

For every `PROVEN` objective/evaluation field record:
- exact static workbook header label text;
- exact header label cell/range;
- exact target cell/merged range;
- exact secured projection path;
- concise evidence showing workbook meaning and secured projection meaning are compatible.

Do NOT infer finer-grained fields when one merged workbook region visually combines multiple business meanings.

Examples requiring caution:
- `Action Plan` does NOT automatically prove secured `kpi`;
- an objectives/target combined region does NOT automatically prove independent `title`, `description`, `target` ownership;
- a shared review/comment region does NOT automatically prove every per-objective `managerComment` owns that same target.

If exact finer-grained ownership is not proven, mark it `UNRESOLVED`.

### G. Reconcile evaluator identity explicitly
Where owner-template labels use terms such as:
- Appraisee;
- 1st Appraiser;
- 2nd Appraiser;
- Chief;
- Manager;
- GM;
- Final;
- Average;

Do NOT translate those labels to `self`, `manager`, `gm`, or `averageScore` merely by naming intuition.

A `PROVEN` evaluator translation requires accepted repository/workflow evidence that establishes the role identity. Cite that authority in the evidence rationale/source. Otherwise keep the semantic translation `UNRESOLVED`.

### H. Rebuild Part A summary/result evidence
Re-inspect exact static labels/merged ranges around relevant summary/result regions, including `BC29:BI35` where applicable.

A production `PROVEN` mapping requires BOTH:
1. workbook static label/merge/accepted template authority proving semantic ownership; and
2. compatible secured projection path.

`Workbook Label Text = N/A` plus a known address and projection path is insufficient by itself. Mark ambiguous cells/ranges `UNRESOLVED`.

Employee-Self confidentiality remains unchanged: semantic compatibility evidence does not authorize rendering fields omitted by secured projection.

### I. Correct Part B competency self/chief semantic evidence
Preserve exact frozen privacy topology:
```text
SELF AREA = K:Q
CHIEF AREA = R:X
```

Do NOT truncate chief authority to `R:W`.

For each relevant competency mapping, distinguish four separate facts:
1. workbook self-rating visual target/merge;
2. workbook chief-rating visual target/merge;
3. exact secured self field/path;
4. exact secured chief field/path ONLY if a stable key/path is proven by current repository authority.

`MboExportService` explicitly allows known self fields for Employee-Self and passes full approver `competencyItems` through without defining a guaranteed chief-rating key schema. Therefore workbook chief semantics may be visually identifiable while production writable chief mapping remains `UNRESOLVED` unless an exact stable chief key/path is independently proven.

Never reconstruct chief/evaluator data filtered from Employee-Self projection.

If exact original per-competency row/block semantic ownership cannot be proven without contradicting frozen rows7:29 dynamic authority, keep per-item mapping unresolved rather than restoring a false uniform-padding model.

### J. Part B summary/comment/signature taxonomy
Retain visually supported summary/comment/signature workbook regions and count-aware relocation only where supported by frozen structural/privacy evidence.

Standalone overall rating/comment/feedback/signature workbook regions whose data source is not exposed by current `MboExportService` must remain:
`NO_SECURED_PROJECTION_SOURCE / DO_NOT_MAP_NO_PROJECTION_SOURCE`.

Do not use those visual regions to invent hidden source data.

### K. Final production-mapping decision table integrity
Final table columns must clearly distinguish:
`Semantic role | Workbook target | Secured projection path | Evidence source | Status | Production decision`

Allowed production decisions:
- `SAFE_TO_MAP` only when status is `PROVEN`;
- `KEEP_UNRESOLVED` when status is `UNRESOLVED`;
- `DO_NOT_MAP_NO_PROJECTION_SOURCE` when status is `NO_SECURED_PROJECTION_SOURCE`.

Mandatory invariants:
- ZERO duplicate exclusive `SAFE_TO_MAP` targets unless an explicit accepted composition rule is cited;
- ZERO `SAFE_TO_MAP` row with null/unknown secured projection path;
- ZERO `PROVEN` claim based only on proximity, current profile code, or inferred alias meaning;
- every relevant secured objective field appears in the evidence, even when unresolved.

### L. Recalculate status counts mechanically
Recalculate the final counts from the final semantic decision rows after all corrections:
- `PROVEN`;
- `UNRESOLVED`;
- `NO_SECURED_PROJECTION_SOURCE`.

Counts must match the document exactly. Do not reuse the prior `21 / 1 / 8` counts unless they are mechanically true after correction.

## 6. Privacy/read-only restrictions
Evidence may contain only privacy-safe template/repository metadata:
- worksheet names;
- static template labels;
- cell/range/merge addresses;
- projection path/field names;
- frozen role evidence;
- status/rationale.

Do NOT copy real employee names, codes, comments, scores, or personal record values into evidence.

Owner XLSX templates are READ-ONLY. Loading for inspection is permitted; saving/writing/mutating is forbidden.

Temporary inspection scripts may exist only outside repository (for example `/tmp`) and must not be committed.

## 7. Explicitly forbidden / out of scope
Do NOT modify:
- `src/profiles/mbo-xlsx-template-profile.js`;
- `tests/mbo-xlsx-template-profile.test.js`;
- `src/services/mbo-export-service.js`;
- feasibility source/tests;
- any Baseline/control document during executor work;
- package files/dependencies;
- `dist/`;
- owner-template XLSX binaries;
- any repository file other than `project-docs/phase-3/evidence/XLSX_TEMPLATE_SEMANTIC_MAPPING_EVIDENCE.md`.

Do NOT:
- create/edit Production XLSX Renderer;
- generate XLSX/PDF/image evidence artifacts;
- mutate workbook cells/styles/merges/defined names/formulas/media/package XML;
- recalculate scoring;
- change authorization logic;
- touch Kintone/ACL/process/customization;
- deploy;
- run Live UAT;
- invoke Claude;
- start Combined Excel parity, PDF parity, export security regression or D3.

## 8. Verification and commit contract
Before commit run:
```bash
git diff --name-only
git status --porcelain
```

Exactly ONE repository path may be changed:
`project-docs/phase-3/evidence/XLSX_TEMPLATE_SEMANTIC_MAPPING_EVIDENCE.md`

Then:
- create exactly ONE R1-R2-R1 evidence-corrective OR blocker commit;
- commit contains exactly the one authorized evidence markdown;
- push to `ai/antigravity-wp002c`;
- STOP immediately;
- do not self-declare semantic evidence/Profile PASS/CLOSED;
- do not edit source after evidence collection;
- do not start the next gate.

Report only:
- commit SHA;
- exact changed file;
- Part A SHA result;
- Part B SHA result;
- final counts of `PROVEN`, `UNRESOLVED`, `NO_SECURED_PROJECTION_SOURCE`;
- duplicate exclusive `SAFE_TO_MAP` target count (must be 0 for a non-blocker evidence submission);
- `git status --porcelain`;
- blocker if any.

## 9. Authorization ledger / exact next action
```text
D2-WP004-R1-SOURCE-TEST-20260902-01 = CONSUMED / CORRECTIVE / DO NOT REUSE
D2-WP004-R1-R1-SOURCE-TEST-20260902-01 = CONSUMED / CORRECTIVE / DO NOT REUSE
D2-WP004-R1-R2-EVIDENCE-20260902-01 = CONSUMED / CORRECTIVE / DO NOT REUSE
D2-WP004-R1-R2-R1-EVIDENCE-20260902-01 = ACTIVE / ONE-SHOT / EVIDENCE-ONLY / EXACT ONE EXISTING MARKDOWN FILE
CONTROL-PLANE-D2-REVIEW-CORRECTIVE-20-ROUND-20260901 = EXHAUSTED / DO NOT REUSE
ACTIVE_D2_SOURCE_CHANGE_AUTH = NONE
ACTIVE_D2_TEST_CHANGE_AUTH = NONE
ACTIVE_D2_EVIDENCE_WRITE_AUTH = D2-WP004-R1-R2-R1-EVIDENCE-20260902-01
ACTIVE_KINTONE_WRITE_AUTH = NONE
ACTIVE_DEPLOY_AUTH = NONE
NEXT_EXECUTOR = ANTIGRAVITY
NEXT_ACTION = EXECUTE ONLY D2-WP004-R1-R2-R1-EVIDENCE-20260902-01; RE-INSPECT EXACT SHA OWNER TEMPLATES READ-ONLY; CORRECT EXACTLY ONE EVIDENCE MARKDOWN; ONE COMMIT; PUSH; REPORT; STOP
ANTIGRAVITY = AUTHORIZED ONLY FOR R1-R2-R1 EVIDENCE
CLAUDE = STOP
KINTONE = NONE
DEPLOY = NONE
PRODUCTION_RENDERER = NOT AUTHORIZED
D3 = HOLD
```
